# Development environment variables (must start with VITE_)
# Backend API URL (use a relative path if you’re proxying /api via Vite)
VITE_BASE_URL=http://127.0.0.1:8000/api/

# Public base path for dev (can be “/” or “./”)
VITE_PUBLIC_PATH=/
